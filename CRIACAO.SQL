-- REMOVE\EXCLUI A TABELA ALUNO
--DROP TABLE ALUNO;

-- CRIA A TABELA ALUNO COM SEUS CAMPOS INICIAIS
CREATE TABLE ALUNO(
    ID_ALUNO INTEGER PRIMARY KEY,
    NOME VARCHAR(100),
    MATRICULA INTEGER UNIQUE
);

-- COMANDO PARA REMOVER\EXCLUIR A COLUNA CPF
--ALTER TABLE ALUNO DROP COLUMN CPF;

-- ALTERA A TABELA ALUNO ADICIONANDO A COLUNA CPF
ALTER TABLE ALUNO ADD COLUMN CPF VARCHAR(11);

-- ALTERA A TABELA ALUNO, ALTERA A COLUNA CPF, ALTERA O TIPO DA COLUNA PARA CHAR
ALTER TABLE ALUNO ALTER COLUMN CPF TYPE CHAR(11);

-- ALTERA A TABELA ALUNO ADICIONANDO O CAMPO RG
ALTER TABLE ALUNO ADD COLUMN RG VARCHAR(12);

-- SELECIONA TODOS OS DADOS DA TABELA ALUNO
SELECT * FROM ALUNO;

-- CRIA A TABELA DE CURSO
CREATE TABLE CURSO(
    ID_CURSO INTEGER PRIMARY KEY,
    NOME VARCHAR(60),
    ID_DEPARTAMENTO INTEGER
);

-- SELECIONA O NOME OS REGISTROS DA TABELA CURSO
SELECT NOME FROM CURSO;
